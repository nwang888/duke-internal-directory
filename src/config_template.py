# this is a template for the config.py file
# replace the empty strings with the appropriate values
# and rename the file to config.py

import os

os.environ['STREAMER_OIT_TOKEN'] = ''
os.environ['CSV_PATH'] = ''
os.environ['YEAR'] = ''